[project]
name = "snowball-rider"
version = "0.1.0"
description = "XRP Trend Follower — snowball growth strategy"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31",
    "python-dotenv>=1.0",
    "structlog>=24.0",
    "websocket-client>=1.7",
]

[tool.setuptools.packages.find]
where = ["src"]
